{
  "name": "better-dispatch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build.js",
    "build:content": "esbuild src/content/content.js --bundle --outfile=dist/content/content.js --format=iife",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@preact/preset-vite": "^2.10.5",
    "preact": "^10.29.3",
    "survey-core": "^2.5.31",
    "survey-js-ui": "^2.5.31"
  }
}
